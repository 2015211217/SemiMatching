import numpy as np
import queue

def InitialSemiMatching(graph, NumberofU):
    M = []
    DegreeU = []

    return M

def AlgorithmSm2(graph):
    Q = queue.Queue()
    ## Compute the initial semi-matching
